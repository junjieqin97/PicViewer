# PicViewer项目开发计划

* 正在开发一个桌面端的照片预览工具，支持打开JPG、PNG、TIFF等常见的图片格式文件，同时支持打开各种相机的RAW文件。

## 基本功能

* 使用该照片预览工具打开照片后，支持显示该照片对应的`直方图`、`波形图`、`元数据`。
* `直方图`和`波形图`支持在`明度模式`和`RGB模式`之间切换，默认显示的是`明度模式`。
* `直方图`和`波形图`在`RGB模式`下，支持在同时显示RGB三个通道和只显示其中一个通道之间切换。
* `元数据`可以像`MacOS`的`预览App`那样显示`通用`、`Exif`、`IPTC`、`TIFF`四种类型的`元数据`，默认显示的是`通用`。
* `波形图`左侧Y轴从下至上显示`曝光值`（分别是0、20、40、60、80、100），其中20、40、60、80的`曝光值`要显示`刻度线`，`曝光值`和`刻度线`用`黄色`显示。
* `直方图`的左上角和右上角有`小三角形`，激活左上角的三角形可以显示照片中`欠曝`的区域，激活右上角的三角形可以显示照片中`过曝`的区域；其中`欠曝`的区域用`绿色`的伪色显示，`过曝`的区域用`红色`的伪色显示。
* `伪色`菜单支持`显示峰值`功能，开启后使用`蓝色`伪色显示图片中的焦点峰值；`显示峰值`下提供`高`、`中`、`低`三个档位，其中`高`更敏感、显示更多峰值，`低`更严格、只显示最强峰值。

## UI优化TODO

### P0：基础体验与可用性

- [x] 优化空态页面：在没有打开图片时，提供更清晰的打开图片/打开文件夹入口、快捷键提示、支持格式提示，并保持文案走 i18n。
- [x] 优化加载态和失败态：图片加载中、预览加载中、加载失败时提供更明确的界面反馈；失败态应显示可理解原因，并避免只显示“加载失败”。
- [x] 优化底部胶卷条：统一缩略图尺寸、间距、选中边框和文件名显示规则，避免 140px 高度下缩略图与文字拥挤。
- [x] 优化右侧信息区的空间利用：减少直方图/波形图 Tab 内的大面积空白，提升分析区域的视觉层次和信息密度。
- [x] 优化元数据表格可读性：固定 Key 列宽、Value 列自适应，长值使用 tooltip 展示，支持清晰的空数据状态。

### P1：分析工具可发现性

- [x] 让当前分析模式可见：在右侧信息区或状态栏显示当前为明度模式/RGB模式，以及当前 RGB 通道。
- [x] 让欠曝/过曝伪色状态可见：除直方图三角形和菜单勾选外，在状态栏或分析区显示当前伪色开关状态。
- [x] 优化直方图伪色三角形：增强未激活/激活/悬停状态的可识别度，并保持欠曝绿色、过曝红色的语义。
- [x] 增加焦点峰值伪色：在伪色菜单下提供高/中/低三档峰值显示，并在伪色状态摘要中展示当前档位。
- [x] 优化缩放反馈：执行放大、缩小、适配窗口后，在状态栏显示当前缩放状态或“适配窗口”状态。

### P2：视觉一致性与主题

- [x] 建立统一 QSS 资源：将当前零散的内联样式收敛到 `ui/resources`，统一图片区、信息区、胶卷条、Tab、表格、状态栏的颜色和间距。
- [x] 优化图片显示画布：使用统一深色背景、边界和滚动区域样式，让图片预览更接近专业照片查看器。
- [x] 优化 Tab 视觉层次：保持图片 Tab 左对齐和可关闭行为，同时提升当前 Tab、悬停 Tab、关闭按钮的识别度。
- [x] 优化菜单文案：将 checkable 菜单项从“显示/隐藏信息区”这类动作式文案调整为更适合勾选状态的名称，例如“信息区”“胶卷窗格”。
- [x] 优化 About 文案：移除 Demo 感描述，改为正式产品说明，并保持中英文翻译同步。

### P3：后续结构优化（需先更新 UI 规范）

- [ ] 评估是否将直方图和波形图合并为一个“分析”面板，以减少 Tab 切换成本；若执行，需先更新 `docs/ui.md`。
- [ ] 评估是否为信息区增加轻量工具栏或分段控件，用于切换明度/RGB 和 RGB 通道；若执行，需先确认不违背菜单栏作为功能入口的规范。
- [x] 评估是否增加胶卷条折叠后的轻量提示或当前文件摘要，避免隐藏胶卷条后上下文丢失。
